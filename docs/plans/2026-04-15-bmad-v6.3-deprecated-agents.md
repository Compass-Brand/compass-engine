# BMAD v6.3.0 — Deprecated Agents Removal Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the three BMAD agents upstream deprecated in v6.3.0 (Quinn/Bob/Barry) from `src/bmad/modules/native/bmm-skills/4-implementation/`, retaining only the Compass-variant Barry under `custom/`.

**Architecture:** Per-agent disposition — (1) `bmad-agent-qa` (Quinn, upstream PR #2179) DELETE from native, no custom override exists and Amelia now owns QA capability; (2) `bmad-agent-sm` (Bob, upstream PR #2186) DELETE from native, no custom override exists and Amelia now owns SP/CS/ER/CC; (3) `bmad-agent-quick-flow-solo-dev` (Barry, upstream PR #2177) DELETE from native but KEEP the existing custom override at `src/bmad/modules/custom/bmm-skills/4-implementation/bmad-agent-quick-flow-solo-dev/` because it wires a Compass-only `bmad-compass-quick-spec` capability. Finally update `default-party.csv` (remove Bob row) and add a `validate.js` guard that fails on reintroduction of any removed native agent.

**Tech Stack:** Node.js, BMAD skill manifests, build.js, validate.js, node:test

**Beads:** bmad-engine-nwyu

---

## Task 1: Delete native `bmad-agent-qa` (Quinn)

**Files:**
- Delete directory: `src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-qa/`
  - `src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-qa/SKILL.md`
  - `src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-qa/bmad-skill-manifest.yaml`

**Step 1 — Remove directory:** `git rm -r src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-qa`

**Step 2 — Validate:** `node tools/validate.js` (should pass; no module-help.csv references)

**Step 3 — Build:** `node tools/build.js`

**Step 4 — Verify:** `grep -c bmad-agent-qa dist/_bmad/_config/skill-manifest.csv` returns `0`; `grep -c bmad-agent-qa dist/_bmad/_config/agent-manifest.csv` returns `0`; `test ! -d dist/_bmad/bmm/4-implementation/bmad-agent-qa`.

**Step 5 — Commit:** `chore(bmad): remove deprecated native bmad-agent-qa (upstream #2179)`

---

## Task 2: Delete native `bmad-agent-sm` (Bob)

**Files:**
- Delete directory: `src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-sm/`

**Step 1 — Remove directory:** `git rm -r src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-sm`

**Step 2 — Validate:** `node tools/validate.js`

**Step 3 — Build:** `node tools/build.js`

**Step 4 — Verify:** `grep -c bmad-agent-sm dist/_bmad/_config/skill-manifest.csv` returns `0`; `test ! -d dist/_bmad/bmm/4-implementation/bmad-agent-sm`.

**Step 5 — Commit:** `chore(bmad): remove deprecated native bmad-agent-sm (upstream #2186)`

---

## Task 3: Delete native `bmad-agent-quick-flow-solo-dev` (Barry) — custom variant retained

**Files:**
- Delete directory: `src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-quick-flow-solo-dev/`
- Leave untouched: `src/bmad/modules/custom/bmm-skills/4-implementation/bmad-agent-quick-flow-solo-dev/` (Compass variant with `QS` → `bmad-compass-quick-spec` capability)

**Step 1 — Remove native directory:** `git rm -r src/bmad/modules/native/bmm-skills/4-implementation/bmad-agent-quick-flow-solo-dev`

**Step 2 — Validate:** `node tools/validate.js` (custom copy must still pass skill-format + manifest-present checks)

**Step 3 — Build:** `node tools/build.js`

**Step 4 — Verify merge semantics:** confirm the custom variant still reaches dist via `mergeModules` in `tools/build.js:265-273` — `test -f dist/_bmad/bmm/4-implementation/bmad-agent-quick-flow-solo-dev/SKILL.md` AND `grep -q "bmad-compass-quick-spec" dist/_bmad/bmm/4-implementation/bmad-agent-quick-flow-solo-dev/SKILL.md` AND `grep -c bmad-agent-quick-flow-solo-dev dist/_bmad/_config/skill-manifest.csv` returns `1`.

**Step 5 — Commit:** `chore(bmad): remove native bmad-agent-quick-flow-solo-dev, retain custom variant (upstream #2177)`

---

## Task 4: Drop sm references from team rosters and prose

**Sequencing note:** Bundle Task 4 with Task 2 if executed in a single PR — otherwise default-party.csv references a non-existent skill between Task 2 and Task 4 commits.

**Files:**
- Modify: `src/bmad/modules/custom/compass-skills/_resources/teams/default-party.csv`
- Modify: `src/bmad/modules/custom/compass-skills/_resources/teams/team-fullstack.yaml` (line 10 still lists `sm`)
- Modify: `src/bmad/modules/custom/bmm-skills/4-implementation/bmad-retrospective/instructions.md` (40+ "Bob (Scrum Master)" persona references)
- Modify: `src/bmad/modules/native/bmm-skills/4-implementation/bmad-retrospective/workflow.md` (128 Bob references — native ships through to dist because custom has no `workflow.md` override)

**Step 1 — Edit `default-party.csv`:** remove the single row beginning `"sm","Bob","Scrum Master"…`. Leave the Barry row (`"quick-flow-solo-dev","Barry",…`) — its `path` column `_bmad/bmm/4-implementation/bmad-agent-quick-flow-solo-dev/SKILL.md` still resolves via the custom merge. Leave the `"creative-problem-solver","Dr. Quinn"…` row untouched (different persona in `cis` module).

**Step 2 — Edit `team-fullstack.yaml`:** remove the `- sm` entry on line 10. Verify no other team YAMLs in `src/bmad/modules/custom/compass-skills/_resources/teams/` reference `sm`.

**Step 3 — Edit both retrospective files:**
- `src/bmad/modules/custom/bmm-skills/4-implementation/bmad-retrospective/instructions.md` (40+ refs)
- `src/bmad/modules/native/bmm-skills/4-implementation/bmad-retrospective/workflow.md` (128 refs — native ships to dist because no custom override)

Replace persona-specific "Bob (Scrum Master)" / "Bob will" / "Bob's role" prose with neutral "Facilitator" / "the agent will" framing. Per architecture: Amelia now owns retrospective (ER capability) — confirm tone matches Amelia's persona elsewhere or keep neutral.

**Allowlist policy:** target is **zero `\bBob\b` matches** in both files post-edit. Dialogue lines like `Alice: "Wait, Bob — I'm seeing..."` (where another persona addresses Bob) must also be neutralized — replace with the new role name.

**Step 4 — Validate:** `node tools/validate.js`

**Step 5 — Build:** `node tools/build.js`

**Step 6 — Verify:** `grep -c '^"sm"' dist/_bmad/compass/_resources/teams/default-party.csv` returns `0`; `grep -c '^"quick-flow-solo-dev"' …` returns `1`; `grep -c '^"creative-problem-solver"' …` returns `1`; `grep -rwl 'Bob' src/bmad/modules/{custom,native}/bmm-skills/4-implementation/bmad-retrospective/ | wc -l` returns `0` (file-list aggregation; `-w` enforces word boundary, `-l` prints filenames-with-matches, final `wc -l` counts files).

**Step 7 — Commit:** `chore(bmad): remove Bob persona references and sm team entries (sm agent removed upstream)`

---

## Task 5: TDD — add `validate.js` guard against reintroducing removed agents (RED)

**Files:**
- Create: `test/test-validate-removed-agents.js`

**Step 1 — RED:** write a failing test that imports a not-yet-exported `validateRemovedAgents` helper from `tools/validate.js`. Three assertions required:
1. **Positive (live tree):** returns `true` when the three deprecated agent directories are absent from `src/bmad/modules/native/bmm-skills/4-implementation/`.
2. **Negative (temp fixture):** returns `false` when a temp fixture native tree contains one of the three offending directories. Use `test/helpers.js::createTempDir` pattern from `test/test-validate-pure.js`.
3. **Error message content:** the false-case error string must cite the relevant upstream PR number (e.g., includes `#2179` for qa, `#2186` for sm, `#2177` for quick-flow-solo-dev) so future maintainers see the rationale.

**Step 2 — Run tests:** `npm run test:unit` — expect failure (`validateRemovedAgents is not a function`).

**Step 3 — Commit (RED):** `test(validate): red test for removed-agent guard`

---

## Task 6: TDD — implement guard (GREEN)

**Files:**
- Modify: `tools/validate.js` (add `validateRemovedAgents` function + export + include in `validate()` `Promise.all`)

**Step 1 — GREEN:** add constant `const REMOVED_NATIVE_AGENTS = ['bmad-agent-qa', 'bmad-agent-sm', 'bmad-agent-quick-flow-solo-dev'];` (with doc-comment citing PRs #2177, #2179, #2186). Implement `validateRemovedAgents(rootDir = ROOT)` checking each does NOT exist under `src/bmad/modules/native/bmm-skills/4-implementation/`. Add to `validate()` `Promise.all` array (line ~362). Export from module footer (line ~388).

**Step 2 — Run tests:** `npm run test:unit` — expect pass.

**Step 3 — Run validate:** `node tools/validate.js` — expect `OK removed-agent guard`.

**Step 4 — Commit (GREEN):** `feat(validate): guard against reintroduction of deprecated BMAD agents`

---

## Task 7: REFACTOR — extract `listDirectorySubdirs` helper (optional)

**Files:**
- Modify: `tools/validate.js`

**Step 1 — REFACTOR:** if the Task-6 implementation duplicates directory-listing logic already present in `listFilesRecursive`, extract a `directoryExists` helper. Keep changes behavior-preserving.

**Step 2 — Run tests:** `npm run test:unit`

**Step 3 — Commit:** `refactor(validate): tidy directory-existence helper`

---

## Task 8: Final end-to-end verification

**Step 1 — Clean build:** `rm -rf dist && node tools/build.js`

**Step 2 — Assert agent count:** count agents in `dist/_bmad/_config/agent-manifest.csv` (header + N rows). Compare to pre-change baseline minus 2 (Quinn + Bob gone from native; Barry count unchanged because custom replaces native).

**Step 3 — Assert client skill generation:** `ls dist/.claude/skills/ | grep -E '^bmad-agent-bmm-(qa|sm)$' | wc -l` returns `0` (anchored regex prevents prefix matches like `bmad-bmm-qa-generate-e2e-tests`); `ls dist/.claude/skills/ | grep '^bmad-agent-bmm-quick-flow-solo-dev$' | wc -l` returns `1`.

**Step 4 — Full test suite:** `npm test` (runs `test:unit` + `validate`) — all green.

**Step 5 — Close beads issue:** `bd close bmad-engine-nwyu` with summary referencing commits.

---

## Sequencing & dependencies

Execution order:

1. **Task 1** (delete qa) — independent, may run first.
2. **Task 2** (delete sm) — independent; may run in parallel with Task 1 on a separate worktree, but simpler to serialize.
3. **Task 3** (delete native quick-flow-solo-dev) — independent of 1 and 2; serializable.
4. **Task 4** (update default-party.csv) — depends on Task 2 landing (conceptually; CSV references sm). Technically the CSV edit alone is safe at any time, but keep after Task 2 so commits read coherently.
5. **Task 5** (RED test) — depends on Tasks 1-3 landing (test asserts the directories are gone; would false-pass against current tree).
6. **Task 6** (GREEN implementation) — depends on Task 5.
7. **Task 7** (REFACTOR) — depends on Task 6; skippable if no duplication emerged.
8. **Task 8** (final verify + close beads) — depends on all prior.

**Parallelizable:** Tasks 1, 2, 3 (three independent directory deletions). Recommend serialization for clean per-PR-cite commit history. Task 7 may be skipped.

**Blocking:** Task 5 MUST land before Task 6 (RED-before-GREEN). Task 8 MUST be last.
